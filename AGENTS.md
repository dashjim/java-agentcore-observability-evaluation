# Repository instructions

- 所有新增或修改代码都应包含清晰中文注释；类、函数、权限与关键分支需要说明意图，避免逐行复述语法。
- Java 应用负责模型调用顺序、工具执行和业务 spans；Bedrock 模型负责工具选择与回答；ADOT Java 负责遥测传输；AgentCore Evaluations 负责读取会话和调用 evaluator。文档和注释必须明确动作所属组件。
- 用户要求保留 AWS 测试资源与结果。禁止自动清理。读取到 `retain_until_user_requests_deletion=true` 的状态文件时，不得删除对应资源；只有用户随后明确要求删除时才可以改变保留策略。
- 状态文件、凭证、原始 telemetry、账号／主机标识和构建缓存不得提交。仅提交匿名样例和经过审核的结果摘要。
- 在工作目录内构建和测试；不要使用系统 `/tmp` 存放任务文件。在本工作区运行时，将 `JAVA_BUILD_DIR` 或工作目录显式设置到 当前工作区的 `tmp/` 下（通过环境变量提供绝对路径）。
- 修改发布内容后执行相应测试、秘密扫描和依赖审查。不得把未检查项写成通过。
